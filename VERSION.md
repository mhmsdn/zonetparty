# ZonetParty MVP v0.4

v0.4 adds deployment-ready configuration for a public HTTPS host (Render) while preserving the v0.3 MCP implementation.

This release is still a prototype: storage is in-memory and authentication/safety infrastructure is not production-ready.
